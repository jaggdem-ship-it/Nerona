import { Vector3, Sprite, SpriteManager, Scene, Observer } from '@babylonjs/core';;/n/n/**/n * Global player state interface. XPDropManager mutates the `xp` field directly./n */nexport interface IPlayerState {/n  xp: number;/n}/n/n/**/n * Anything with a readable world-space position vector (AbstractMesh, TransformNode, etc.)/n */ninterface IPositionProvider {/n  readonly position: Vector3;/n}/n/n/**/n * Runtime configuration for gem behaviour./n */nexport interface XPDropConfig {/n  /** Distance at which gems begin accelerating toward the player. */n  readonly magnetRadius: number;/n  /** Distance at which gems are collected (should match player capsule radius). */n  readonly collectRadius: number;/n  /** m/s² acceleration while magnetized. */n  readonly magnetAcceleration: number;/n  /** Terminal velocity while magnetized. */n  readonly maxMagnetSpeed: number;/n  /** Base experience granted per gem. */n  readonly baseXPValue: number;/n  /** Random XP variance added to base value. */n  readonly xpVariance: number;/n  /** Vertical bob frequency for idle gems (radians/sec). */n  readonly idleBobSpeed: number;/n  /** Vertical bob amplitude for idle gems (world units). */n  readonly idleBobHeight: number;/n}/n/n/**/n * Internal pooled object representing a single Soul Gem on the floor./n */ninterface SoulGem {/n  /** Babylon sprite instance. */n  sprite: Sprite;/n  /** Current world-space velocity (m/s). */n  velocity: Vector3;/n  /** How much XP this gem is worth. */n  xpValue: number;/n  /** True when this gem is currently in the active simulation array. */n  isActive: boolean;/n  /** True once the player enters the magnet radius; persists until collected. */n  isMagnetized: boolean;/n  /** Used for idle bobbing phase offset. */n  spawnTime: number;/n  /** Original Y position so bobbing doesn't drift. */n  initialY: number;/n}/n/nexport class XPDropManager {/n  private readonly scene: Scene;/n  private readonly spriteManager: SpriteManager;/n  private readonly player: IPositionProvider;/n  private readonly playerState: IPlayerState;/n  private readonly config: XPDropConfig;/n/n  private activeGems: SoulGem[] = [];/n  private gemPool: SoulGem[] = [];/n  private renderObserver: Observer<Scene> | null = null;/n/n  /** Optional hook fired the moment a gem is collected. */n  private onCollectAudio?: () => void;/n/n  constructor(/n    scene: Scene,/n    spriteManager: SpriteManager,/n    player: IPositionProvider,/n    playerState: IPlayerState,/n    config?: Partial<XPDropConfig>/n  ) {/n    this.scene = scene;/n    this.spriteManager = spriteManager;/n    this.player = player;/n    this.playerState = playerState;/n/n    this.config = {/n      magnetRadius: 3.0,/n      collectRadius: 0.4,/n      magnetAcceleration: 22.0,/n      maxMagnetSpeed: 16.0,/n      baseXPValue: 10,/n      xpVariance: 5,/n      idleBobSpeed: 2.5,/n      idleBobHeight: 0.08,/n      ...config,/n    };/n/n    // Hook into the main render loop./n    this.renderObserver = this.scene.onBeforeRenderObservable.add(() => {/n      this.update();/n    });/n  }/n/n  /**/n   * Spawn a Soul Gem at the given world position./n   * @param positionVector World-space drop position./n   * @param xpCoefficient Multiplier applied to the randomized base XP value./n   */n  public spawnGem(positionVector: Vector3, xpCoefficient: number = 1.0): void {/n    const gem = this.acquireGem();/n/n    gem.sprite.position.copyFrom(positionVector);/n    gem.sprite.isVisible = true;/n    gem.sprite.width = 0.5;/n    gem.sprite.height = 0.5;/n/n    gem.velocity.setAll(0);/n    gem.xpValue = Math.floor(/n      (this.config.baseXPValue + Math.random() * this.config.xpVariance) */n        xpCoefficient/n    );/n    gem.isActive = true;/n    gem.isMagnetized = false;/n    gem.spawnTime = performance.now() / 1000;/n    gem.initialY = positionVector.y;/n/n    this.activeGems.push(gem);/n  }/n/n  /** Register a callback that fires on every gem collection (e.g. audio cue). */n  public setAudioCallback(callback: () => void): void {/n    this.onCollectAudio = callback;/n  }/n/n  /** Permanently tears down the manager and all pooled sprites. */n  public dispose(): void {/n    if (this.renderObserver) {/n      this.scene.onBeforeRenderObservable.remove(this.renderObserver);/n      this.renderObserver = null;/n    }/n/n    // Dispose every Babylon sprite to free GPU memory./n    for (const gem of this.activeGems) gem.sprite.dispose();/n    for (const gem of this.gemPool) gem.sprite.dispose();/n/n    this.activeGems = [];/n    this.gemPool = [];/n  }/n/n  /* ------------------------------------------------------------------ */n  /*  Private helpers                                                    */n  /* ------------------------------------------------------------------ */n/n  private acquireGem(): SoulGem {/n    const recycled = this.gemPool.pop();/n    return recycled ?? this.createGem();/n  }/n/n  private createGem(): SoulGem {/n    const sprite = new Sprite('soulGem', this.spriteManager);/n    sprite.isVisible = false;/n    return {/n      sprite,/n      velocity: new Vector3(0, 0, 0),/n      xpValue: 0,/n      isActive: false,/n      isMagnetized: false,/n      spawnTime: 0,/n      initialY: 0,/n    };/n  }/n/n  private update(): void {/n    const dt = this.scene.getEngine().getDeltaTime() / 1000.0;/n    const now = performance.now() / 1000;/n/n    const playerPos = this.player.position;/n    const magnetRadiusSq =/n      this.config.magnetRadius * this.config.magnetRadius;/n    const collectRadiusSq =/n      this.config.collectRadius * this.config.collectRadius;/n/n    // Iterate backwards so we can splice collected gems in O(1)./n    for (let i = this.activeGems.length - 1; i >= 0; i--) {/n      const gem = this.activeGems[i];/n      const gemPos = gem.sprite.position;/n/n      // ---- Squared distance check (no sqrt until necessary) ----/n      const dx = playerPos.x - gemPos.x;/n      const dy = playerPos.y - gemPos.y;/n      const dz = playerPos.z - gemPos.z;/n      const distSq = dx * dx + dy * dy + dz * dz;/n/n      // ---- Collection ----/n      if (distSq < collectRadiusSq) {/n        this.collectGem(gem, i);/n        continue;/n      }/n/n      // ---- Magnetic pull state ----/n      if (distSq < magnetRadiusSq || gem.isMagnetized) {/n        gem.isMagnetized = true;/n/n        // Normalized direction toward player./n        const dist = Math.sqrt(distSq);/n        const invDist = 1.0 / dist;/n        const dirX = dx * invDist;/n        const dirY = dy * invDist;/n        const dirZ = dz * invDist;/n/n        // Accelerate velocity vector toward player./n        gem.velocity.x += dirX * this.config.magnetAcceleration * dt;/n        gem.velocity.y += dirY * this.config.magnetAcceleration * dt;/n        gem.velocity.z += dirZ * this.config.magnetAcceleration * dt;/n/n        // Clamp to max speed./n        const speedSq =/n          gem.velocity.x * gem.velocity.x +/n          gem.velocity.y * gem.velocity.y +/n          gem.velocity.z * gem.velocity.z;/n        const maxSpeedSq =/n          this.config.maxMagnetSpeed * this.config.maxMagnetSpeed;/n        if (speedSq > maxSpeedSq) {/n          const scale = this.config.maxMagnetSpeed / Math.sqrt(speedSq);/n          gem.velocity.x *= scale;/n          gem.velocity.y *= scale;/n          gem.velocity.z *= scale;/n        }/n      } else {/n        // Idle visual feedback: gentle vertical hover./n        const bobOffset =/n          Math.sin((now - gem.spawnTime) * this.config.idleBobSpeed) */n          this.config.idleBobHeight;/n        gemPos.y = gem.initialY + bobOffset;/n      }/n/n      // ---- Integrate velocity ----/n      if (gem.isMagnetized) {/n        gemPos.x += gem.velocity.x * dt;/n        gemPos.y += gem.velocity.y * dt;/n        gemPos.z += gem.velocity.z * dt;/n      }/n    }/n  }/n/n  private collectGem(gem: SoulGem, activeIndex: number): void {/n    // 1. Credit global player state./n    this.playerState.xp += gem.xpValue;/n/n    // 2. Audio cue hook./n    if (this.onCollectAudio) {/n      this.onCollectAudio();/n    }/n/n    // 3. Remove from active simulation array./n    this.activeGems.splice(activeIndex, 1);/n/n    // 4. Reset state and push back into the object pool (prevents memory leaks / GC stutter)./n    gem.isActive = false;/n    gem.isMagnetized = false;/n    gem.velocity.setAll(0);/n    gem.sprite.isVisible = false;/n    this.gemPool.push(gem);/n  }/n}/n