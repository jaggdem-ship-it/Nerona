/* ===== OBJECT POOL ===== */export class ObjectPool{constructor(createFn,resetFn,initialSize=50){this.createFn=createFn;this.resetFn=resetFn;this.available=[];this.active=[];for(let i=0;i<initialSize;i++){const obj=this.createFn();obj._poolActive=false;this.available.push(obj)}}acquire(){let obj;if(this.available.length>0){obj=this.available.pop()}else{obj=this.createFn()}obj._poolActive=true;this.active.push(obj);return obj}release(obj){if(!obj||!obj._poolActive)return;obj._poolActive=false;this.resetFn(obj);const idx=this.active.indexOf(obj);if(idx>-1)this.active.splice(idx,1);this.available.push(obj)}releaseAll(){for(const obj of this.active){obj._poolActive=false;this.resetFn(obj);this.available.push(obj)}this.active.length=0}getActive(){return this.active}size(){return this.available.length+this.active.length}}